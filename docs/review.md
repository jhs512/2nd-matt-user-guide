# 검토 결과

기준: 설계 문서만 있는 094522c부터 구현 bb33b95까지. `/code-review` 지시에 따라 두 검토를 별도 에이전트로 수행했습니다.

## Standards

명시된 규칙 위반 없음. HTTP/H2·화면·브라우저 검사, 메모리 토큰, 로컬 이슈 관리와 검증 후 배포 순서를 확인했습니다. App.tsx가 여러 화면과 인증·API 호출을 함께 담당한다는 유지보수 개선 의견 1개가 있었습니다. 작은 실습에서는 유지하되 기능 확대 시 분리를 검토합니다.

## Spec

1. 저장 중 새 공지로 이동하면 작성 내용이 지워질 수 있음 → 저장 중 해당 버튼 비활성화, 지연된 실패 응답으로 회귀 테스트 추가.
2. 만료 토큰을 공개 상세 GET에도 보내면 읽기 실패 → 공개 GET에는 Authorization 제외.
3. 문서에 없는 page 상한이 존재 → 음수만 거절하고 DB 개수보다 큰 페이지는 빈 배열 반환.

Standards 규칙 위반 0건·개선 의견 1건, Spec 3건 수정. 운영 배포는 외부 한도·설정 미비로 여전히 미완료입니다.
